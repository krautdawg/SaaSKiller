<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your SaaSKiller Audit</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #111827;
      background-color: #f9fafb;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 40px auto;
      background-color: #ffffff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .header {
      background-color: #DC2626;
      color: #ffffff;
      padding: 32px 24px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 32px;
      font-weight: 700;
    }
    .header p {
      margin: 8px 0 0 0;
      font-size: 14px;
      opacity: 0.9;
    }
    .content {
      padding: 32px 24px;
    }
    .greeting {
      font-size: 18px;
      font-weight: 600;
      margin-bottom: 16px;
    }
    .intro {
      color: #6B7280;
      margin-bottom: 24px;
    }
    .bleed-box {
      background-color: #FEE2E2;
      border-left: 4px solid #DC2626;
      padding: 20px;
      margin: 24px 0;
      border-radius: 8px;
    }
    .bleed-label {
      font-size: 14px;
      color: #991B1B;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 8px;
    }
    .bleed-amount {
      font-size: 36px;
      font-weight: 700;
      color: #DC2626;
      margin: 8px 0;
    }
    .bleed-note {
      font-size: 12px;
      color: #991B1B;
      font-style: italic;
    }
    .savings-box {
      background-color: #D1FAE5;
      border-left: 4px solid: #059669;
      padding: 20px;
      margin: 24px 0;
      border-radius: 8px;
    }
    .savings-label {
      font-size: 14px;
      color: #065F46;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 8px;
    }
    .savings-amount {
      font-size: 28px;
      font-weight: 700;
      color: #059669;
      margin: 8px 0;
    }
    .metric {
      margin: 12px 0;
      color: #6B7280;
    }
    .metric strong {
      color: #111827;
    }
    .section {
      margin: 32px 0;
    }
    .section-title {
      font-size: 16px;
      font-weight: 700;
      color: #111827;
      margin-bottom: 12px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .feature-list {
      list-style: none;
      padding: 0;
    }
    .feature-list li {
      padding: 8px 0;
      color: #6B7280;
      border-bottom: 1px solid #E5E7EB;
    }
    .feature-list li:last-child {
      border-bottom: none;
    }
    .feature-list li:before {
      content: "✓ ";
      color: #059669;
      font-weight: 700;
      margin-right: 8px;
    }
    .cta {
      background-color: #DC2626;
      color: #ffffff;
      text-align: center;
      padding: 16px;
      border-radius: 8px;
      margin: 32px 0;
    }
    .cta a {
      color: #ffffff;
      text-decoration: none;
      font-weight: 600;
      font-size: 16px;
    }
    .footer {
      background-color: #F9FAFB;
      padding: 24px;
      text-align: center;
      color: #9CA3AF;
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <h1>SaaSKiller</h1>
      <p>Your {{toolName}} Audit is Ready</p>
    </div>

    <!-- Content -->
    <div class="content">
      <div class="greeting">Hey {{name}},</div>
      <p class="intro">
        We've crunched the numbers on {{toolName}}. Here's what your SaaS addiction is costing you:
      </p>

      <!-- The 3-Year Bleed -->
      <div class="bleed-box">
        <div class="bleed-label">The 3-Year Bleed</div>
        <div class="bleed-amount">${{bleedAmount}}</div>
        <div class="bleed-note">Money gone forever.</div>
      </div>

      <!-- Build It Yourself -->
      <div class="savings-box">
        <div class="savings-label">Build It Yourself</div>
        <div class="savings-amount">${{savingsAmount}} saved</div>
        <div class="metric"><strong>Build Cost:</strong> ${{buildCostMin}} - ${{buildCostMax}}</div>
        <div class="metric"><strong>ROI:</strong> {{roiMonths}} months</div>
      </div>

      <!-- Features Kept -->
      {{#if featuresKept.length}}
      <div class="section">
        <div class="section-title">Features You Keep</div>
        <ul class="feature-list">
          {{#each featuresKept}}
          <li>{{this.name}}</li>
          {{/each}}
        </ul>
      </div>
      {{/if}}

      <!-- Features Removed -->
      {{#if featuresRemoved.length}}
      <div class="section">
        <div class="section-title">Bloat You Cut</div>
        <ul class="feature-list">
          {{#each featuresRemoved}}
          <li>{{this.name}}</li>
          {{/each}}
        </ul>
      </div>
      {{/if}}

      <!-- Custom Features -->
      {{#if customFeatures.length}}
      <div class="section">
        <div class="section-title">Custom Features Added</div>
        <ul class="feature-list">
          {{#each customFeatures}}
          <li>{{this.name}} ({{this.estimatedHours}}h)</li>
          {{/each}}
        </ul>
      </div>
      {{/if}}

      <!-- CTA -->
      <div class="cta">
        <p style="margin: 0 0 12px 0; font-size: 14px;">Your detailed audit report is attached as PDF</p>
        <a href="https://saaskiller.com">Stop Renting. Start Owning. →</a>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p>Generated by SaaSKiller</p>
      <p>Stop paying rent. Build what you need.</p>
    </div>
  </div>
</body>
</html>
